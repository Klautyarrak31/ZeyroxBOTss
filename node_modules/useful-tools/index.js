class index {
	static tarih(date, format) {
		const aylar = {
			"1": "Ocak",
			"2": "Şubat",
			"3": "Mart",
			"4": "Nisan",
			"5": "Mayıs",
			"6": "Haziran",
			"7": "Temmuz",
			"8": "Ağustos",
			"9": "Eylül",
			"10": "Ekim",
			"11": "Kasım",
			"12": "Aralık"
		}
		const gunler = {
			"0": "Pazar",
			"1": "Pazartesi",
			"2": "Salı",
			"3": "Çarşamba",
			"4": "Perşembe",
			"5": "Cuma",
			"6": "Cumartesi",
		}
		const x = new Date(date)
		const tarih new Date(Date.UTC(x.getFullYear(), x.getMonth() + 1, x.getDay(), x.getHours(), x.getMinutes(), x.getSeconds()))
		const gun = tarih.getDay()
		const ay = tarih.getMonth() + 1
		const yil = tarih.getFullYear()
		const saat = `${tarih.getHours() < 10 ? "0" + tarih.getHours() : tarih.getHours()}:${tarih.getMinutes() < 10 ? "0" + tarih.getMinutes() : tarih.getMinutes()}:${tarih.getSeconds() < 10 ? "0" + tarih.getSeconds() : tarih.getSeconds()}`
		if(format) {
			const y = format.replace("G", tarih.getDate() < 10 ? "0" + tarih.getDate() : tarih.getDate()).replace("A", aylar[ay]).replace("Y", yil).replace("g", gunler[gun]).replace("S", tarih.getHours() < 10 ? "0" + tarih.getHours() : tarih.getHours()).replace("D", tarih.getMinutes() < 10 ? "0" + tarih.getMinutes() : tarih.getMinutes()).replace("s", tarih.getSeconds() < 10 ? "0" + tarih.getSeconds() : tarih.getSeconds())
			return y
		} else {
			return `${gunt} ${aylar[ay]} ${yil} ${gunler[gun]} ${saat}`
		}
	}
}

module.exports = index;